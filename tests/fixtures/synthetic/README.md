# Synthetic fixtures only

Everything under this directory must be manually constructed and must not be
derived from a real patient, EHR, research dataset, production prompt, audit
log, DICOM object or breach report.

Format-valid identifiers used by detector tests must be documented in the test
as either generated synthetic values or public standards examples. Never add a
real person's identifier merely because it is already publicly accessible.
